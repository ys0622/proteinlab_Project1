export const metadata = {
  title: "등급 기준 | ProteinLab",
  description: "단백질 음료·단백질 바 등급 산정 기준",
};

export default function GradeCriteriaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
